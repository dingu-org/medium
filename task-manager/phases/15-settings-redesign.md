# Phase 15 — Settings redesign & PT controls

**Goal.** Replace the single-scroll `/settings` with the designed "Ti" hub +
sub-screens and add the new PT controls (global assistant pause, merged
personal/practice profile, service prices, full WhatsApp connection screen,
grouped notifications, account & data).

**All detail lives in Notion (teamspace Medium).** Do not duplicate it here.

- **Spec (Document Hub):** [Phase 15 — Settings redesign & PT controls (spec)](https://app.notion.com/p/39b0e1f4dd1081ecb2a6e19cc86f00a1)
  — decisions log, pause semantics, IA/routes, per-screen spec, migration 0018,
  backend changes, non-negotiables, verification, out-of-scope.
- **Tasks (Tasks Tracker):** 14 execution tasks titled `Phase 15 · …`,
  all unassigned / Not started. Work them via the tracker; update status there.

**Design source of truth:** Claude Design canvas **Medium - Settings.html**
(18 artboards; sources `settings/*.jsx`). The committed
`docs/design/medium4/project/` export predates this canvas — re-export it (or
read via DesignSync) before implementing.
